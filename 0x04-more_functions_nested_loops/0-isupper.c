#include "main.h"

/**
 * _isupper - checks for number if its upper.
 * Return: 1 if is upper, 0 otherwise.
 * @c: the character to be checked.
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else 
	{
		return (0);
	}

	return (0);
}
